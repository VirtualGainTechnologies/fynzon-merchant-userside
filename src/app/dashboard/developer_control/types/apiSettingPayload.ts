export interface ApiSettingPayload {
  mode: string;
  websiteURL?: string;
  apiKey?: string;
  ipAddress?: string;
  otp?: number;
  otpId?: string;
  url?: string;
  oldUrl?: string;
  event?: string;
  oldIpAddress?: string;
}
